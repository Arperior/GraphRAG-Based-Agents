# ======================= Relation Extraction (Optimized) =======================

Extract relationships between entities.
Return ONLY a valid JSON array.

Rules:
1. "evidence" must be a SHORT snippet (Max 12 words).
2. "confidence" is a float (0.0 to 1.0).
3. No extra text or explanations.

JSON Structure:
[
  {
    "source": "Entity1",
    "target": "Entity2",
    "relation": "RELATION_TYPE",
    "evidence": "short text snippet",
    "confidence": 0.9
  }
]

Text:
{input_text}