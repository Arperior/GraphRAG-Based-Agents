# ======================= Entity Extraction (Optimized) =======================

You are an expert Information Extraction system.
Your task is to identify key entities in the provided text matching the target types.

### TARGET ENTITY TYPES
{entity_types}

### RULES
1. **Format**: Return ONLY a valid JSON array. No markdown code blocks (```json).
2. **Precision**: "name" must be the exact text from the document.
3. **Conciseness**: "description" must be MAX 8 WORDS.
4. **Filtering**: Do not extract generic nouns (e.g., "the report", "data", "we"). Focus on specific named entities (people, organizations, locations, metrics).

### JSON STRUCTURE
[
  {{
    "name": "Exact Entity Name",
    "type": "TYPE",
    "description": "Short description (max 8 words)"
  }}
]

### INPUT TEXT
{input_text}