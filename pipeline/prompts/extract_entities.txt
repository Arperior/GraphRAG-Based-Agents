# ======================= Entity Extraction (Optimized) =======================

Extract named entities (persons, places, organizations).
Return ONLY a valid JSON array.

Rules:
1. "description" must be MAX 8 WORDS.
2. No extra commentary.

JSON Structure:
[
  {
    "name": "Entity Name",
    "type": "TYPE",
    "description": "Short description"
  }
]

Text:
{input_text}